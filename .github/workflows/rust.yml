name: Rust

on:
  push:
    branches: [ "gui"]
  pull_request:
    branches: [ "gui" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: cargo build --verbose
